<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title>Document</title>
</head>
<body>
    <%- include("./partials/flash") %>
    <div class="w-full min-h-screen bg-zinc-800 p-10 text-white">
        <div class="w-full flex justify-between mb-5">
            <h3 class="text-3xl"><span class="font-lighter">Edit Your Post </span> 📝</h3> 
            <a class="bg-red-400 text-sm inline block rounded-md px-3 py-2">Log Out</a>
        </div>
        
        <form action="/update/<%=post._id%>" method="post">
             <textarea name="content" class="block p-2 w-1/3 resize-none bg-transparent border-zinc-600 border-[1px] text-zinc-500"><%=post.content%></textarea>   
             <div class="flex mt-3">
                <input type="submit" class="text-sm text-black px-3 py-2 w-30 bg-yellow-500 block rounded-md outline-none" value="Update Post">           
             <a class="text-sm text-blue-500 underline ml-4 block mt-2" href="/profile">back to profile page</a>
             </div>           
        </form>
        
        </div>
    </div>
</body>
</html> -->











<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  <title>Edit</title>
</head>

<body class="bg-gray-100 min-h-screen flex items-center justify-center">
  <%- include("./partials/flash") %>

  <div class="bg-white p-6 rounded-xl shadow w-full max-w-lg">

    <h3 class="text-xl font-semibold mb-4">Edit Post ✏️</h3>

    <form action="/update/<%=post._id%>" method="post">
      <textarea name="content"
        class="w-full border rounded-lg p-3 resize-none focus:ring-2 focus:ring-indigo-400 outline-none"><%=post.content%></textarea>

      <div class="flex flex-col sm:flex-row gap-3 mt-4">
        <button class="bg-yellow-400 px-4 py-2 rounded-lg">
          Update
        </button>
        <a href="/profile" class="text-indigo-600 underline mt-2">
          Back
        </a>
      </div>
    </form>
  </div>
</body>
</html>
