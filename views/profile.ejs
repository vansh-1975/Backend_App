<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title>Document</title>
</head>
<body>
    <%- include("./partials/flash") %>

    <div class="w-full min-h-screen bg-zinc-800 p-10 text-white">
        <div class="flex justify-between">
            <div class="flex">
            <div class="w-11 h-11 rounded-md mr-3 overflow-hidden">
                <img src="/images/uploads/<%=user.profilepic%>">
            </div>
        <h3 class="text-3xl"><span class="font-lighter">Hello,</span> <%=user.username%> !! 👋</h3> 
        </div>
            <a href="/logout" class="bg-red-600 rounded-lg items-center px-3 py-2 text-sm">Log Out</a>
        </div>
            <a class="text-blue-500 block mt-2 mb-6" href="/upload">Edit Profile Pic</a>
        <form action="/post" method="post">
             <textarea name="content" placeholder="What's in your mind?" class="block p-2 w-1/3 resize-none bg-transparent border-zinc-600 border-[1px]"></textarea>
             <div class="flex gap-2">  
             <input type="submit" class="text-sm px-3 py-2 w-40 bg-green-700 block rounded-md outline-none mt-3" value="Create New Post">
             <input type="submit" formaction="/allUsers" formmethod="get" class="text-sm px-3 py-2 w-40 bg-green-700 block rounded-md outline-none mt-3" value="See All Posts">
             </div>            
        </form>
        

            <h3 class="text-zinc-300 mt-20 mb-5">Your Posts</h3>
            <div class="posts grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
            
            <% if (user.posts.length > 0) {%>
                <%user.posts.reverse().forEach(post=>{%>
                    <div class="postcontainer p-4 border-[1px] border-zinc-700 bg-zinc-800 rounded-md">
            <div class="flex justify-between">
                <h4 class="text-blue-500 mb-3">@<%=user.username%></h4>
                <small class="text-zinc-400 font-bold tracking-tighter mb-3"><%= post.date.toDateString() %></small>
            </div>
            
            <p class="text-sm tracking-tighter"><%=post.content%></p>
            <p class="text-[15px] mt-4 ml-2"><%=post.likes.length%></p>
            <div class="btns flex">
                <p class="text-pink-500">
                    Likes
                </p>
                <a class="ml-5 text-zinc-500" href="/edit/<%=post._id%>">Edit</a>
                <a class="ml-5 text-red-500" href="/remove/<%=post._id%>">Remove</a>
            </div>
            </div>
                      
                <%})%>
            <%}
             else { %>
                   <p class="text-zinc-500 mt-5">No posts yet</p>
                <% 
            } %>

        </div>
    </div>
</body>
</html> -->










<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  <title>Profile</title>
</head>

<body class="bg-gray-100">
  <%- include("./partials/flash") %>

  <div class="max-w-6xl mx-auto p-4 sm:p-8">

    <div class="flex flex-col sm:flex-row justify-between items-center gap-4 mb-6">
      <div class="flex items-center gap-3">
        <img src="/images/uploads/<%=user.profilepic%>"
          class="w-12 h-12 rounded-full object-cover">
        <h3 class="text-xl font-semibold text-gray-800">
          Hello, <%=user.username%> 👋
        </h3>
      </div>

      <a href="/logout"
        class="bg-red-500 hover:bg-red-600 text-white px-4 py-2 rounded-lg text-sm">
        Logout
      </a>
    </div>

    <a href="/upload" class="text-indigo-600 text-sm underline mb-4 block">
      Edit Profile Picture
    </a>

    <form action="/post" method="post" class="bg-white p-4 rounded-xl shadow mb-10">
      <textarea name="content"
        placeholder="What's on your mind?"
        class="w-full border rounded-lg p-3 resize-none focus:ring-2 focus:ring-indigo-400 outline-none"></textarea>

      <div class="flex flex-col sm:flex-row gap-3 mt-3">
        <input type="submit" value="Create Post"
          class="bg-indigo-600 text-white px-4 py-2 rounded-lg">
        <input type="submit" formaction="/allUsers" formmethod="get"
          value="See All Posts"
          class="border border-indigo-600 text-indigo-600 px-4 py-2 rounded-lg">
      </div>
    </form>

    <h3 class="text-gray-700 font-semibold mb-4">Your Posts</h3>

    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
      <% if (user.posts.length > 0) { %>
        <% user.posts.reverse().forEach(post => { %>

        <div class="bg-white p-4 rounded-xl shadow">
          <div class="flex justify-between mb-2">
            <span class="text-indigo-600 font-medium">@<%=user.username%></span>
            <small class="text-gray-400"><%= post.date.toDateString() %></small>
          </div>

          <p class="text-gray-700 text-sm"><%=post.content%></p>

          <div class="flex justify-between items-center mt-4 text-sm">
            <span>❤️ <%=post.likes.length%></span>
            <div class="flex gap-3">
              <a href="/edit/<%=post._id%>" class="text-indigo-600">Edit</a>
              <a href="/remove/<%=post._id%>" class="text-red-500">Delete</a>
            </div>
          </div>
        </div>

        <% }) %>
      <% } else { %>
        <p class="text-gray-500">No posts yet</p>
      <% } %>
    </div>

  </div>
</body>
</html>
