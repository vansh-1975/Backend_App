<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title>Document</title>
</head>
<body>

    <%- include("./partials/flash") %>

    <section class="text-gray-600 body-font">
  <div class="container px-5 py-24 mx-auto">
    <a class="bg-zinc-600 px-4 py-2 rounded-lg text-white tracking-tighter" href="/profile" >Back to profile</a>
    <div class="flex flex-wrap w-full mb-20 flex-col items-center text-center">
      <h1 class="sm:text-3xl text-2xl font-medium title-font mb-2 text-gray-900">All Posts</h1>
      <p class="lg:w-1/2 w-full leading-relaxed text-gray-500">Explore thoughts, ideas, and moments shared by the community users</p>
    </div>
    <div class="flex flex-wrap -m-4">
<% if(posts.length===0){
    %>
      <p class="text-zinc-500 mt-5">No posts yet</p>
    <%
}else{
    %>
      <%posts.forEach(post => {
        %>
          <div class="xl:w-1/3 md:w-1/2 p-4">
        <div class="border border-gray-200 p-6 rounded-lg">
            <div class="flex justify-between">
            <div class="w-10 h-10 rounded-full overflow-hidden mr-3">
            <img
            src="/images/uploads/<%= post.user.profilepic %>"
            class="w-full h-full object-cover block"
            alt="profile">
            </div>

          <small class="text-zinc-400 font-bold tracking-tighter mb-3"><%= post.date.toDateString() %></small>
          </div>
          <h2 class="text-lg text-gray-900 font-medium title-font mb-2"><%=post.user.username%></h2>
          <p class="leading-relaxed text-base"><%=post.content%></p>
          <p class="text-[15px] mt-4 ml-2"><%=post.likes.length%></p>
        <a class="text-pink-500" href="/like/<%= post._id %>">
            <%= post.likes.includes(loggedInUserId) ? "Unlike" : "Like" %>
        </a>

        </div>
      </div>
        <%
      })%>
    <%
} %>

</section>
</body>
</html> -->












<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  <title>All Posts</title>
</head>

<body class="bg-gray-100">
  <%- include("./partials/flash") %>

  <div class="max-w-6xl mx-auto p-4 sm:p-8">
    <a href="/profile" class="text-indigo-600 underline mb-4 block">
      ← Back to Profile
    </a>

    <h1 class="text-2xl font-bold text-center mb-8">Community Posts</h1>

    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
      <% if(posts.length === 0){ %>
        <p class="text-gray-500">No posts yet</p>
      <% } else { %>
        <% posts.forEach(post => { %>

        <div class="bg-white p-4 rounded-xl shadow">
          <div class="flex items-center gap-3 mb-2">
            <img src="/images/uploads/<%= post.user.profilepic %>"
              class="w-10 h-10 rounded-full object-cover">
            <div>
              <p class="font-semibold"><%=post.user.username%></p>
              <small class="text-gray-400"><%= post.date.toDateString() %></small>
            </div>
          </div>

          <p class="text-gray-700 text-sm mb-3"><%=post.content%></p>

          <a href="/like/<%= post._id %>" class="text-pink-500">
            ❤️ <%= post.likes.includes(loggedInUserId) ? "Unlike" : "Like" %>
          </a>
        </div>

        <% }) %>
      <% } %>
    </div>
  </div>
</body>
</html>
